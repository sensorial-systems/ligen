use ligen::ir::Project;
use std::convert::TryFrom;

#[test]
fn project() {
    // let path = std::path::Path::new("test-project");
    // let project = Project::try_from(path).expect("Failed to get the project from the specified path.");
    // assert_eq!(project.arguments.crate_name, "test-project");
    // assert_eq!(project.arguments.manifest_path, );
    // assert_eq!(project.arguments.build_type, );
    // assert_eq!(project.arguments.manifest_path, );
    // assert_eq!(project.arguments.target_dir, );
    // assert_eq!(project.arguments.workspace_member_package_id, );
    // assert_eq!(project.arguments.workspace_path, );
}
