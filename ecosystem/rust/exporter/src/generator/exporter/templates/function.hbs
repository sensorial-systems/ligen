#[no_mangle]
pub extern "C" fn {{ join_path "_" path }}({{>parameters inputs}}) -> {{mapped_type output}} {
    let result = {{ join_path "::" path }}({{>arguments inputs}});
    {{ marshal_output output }}
}
