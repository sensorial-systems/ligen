#pragma once

{{#each modules}}
#include <{{join_path "/" this.path}}.h>
{{/each}}

{{#if functions}}
extern "C" {
{{/if}}
{{#each functions}}
{{>function this }}
{{/each}}
{{#if functions}}
}
{{/if}}
