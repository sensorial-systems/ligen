# Auto-generated by ligen-cmake {generator_version}

CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT("{project_name}")

IF(TARGET ${{PROJECT_NAME}})
RETURN()
ENDIF()

# INTERFACE is used to create a header-only library.
ADD_LIBRARY(${{PROJECT_NAME}} INTERFACE)
TARGET_INCLUDE_DIRECTORIES(${{PROJECT_NAME}} INTERFACE include)

IF(UNIX)
    TARGET_LINK_LIBRARIES(${{PROJECT_NAME}} INTERFACE "${{CMAKE_CURRENT_SOURCE_DIR}}/lib/lib${{PROJECT_NAME}}.a" -pthread -Wl,--no-as-needed -ldl -lm)
ELSEIF(WIN32)
    TARGET_LINK_LIBRARIES(${{PROJECT_NAME}} INTERFACE "${{CMAKE_CURRENT_SOURCE_DIR}}/lib/${{PROJECT_NAME}}.lib" ws2_32 userenv)
ENDIF()